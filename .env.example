# 本项目 v1 不需要任何服务端环境变量：
# Base URL / API Key / 模型名 全部由用户在 UI 手填、存浏览器 localStorage，
# 经 /api/translate 转发路由穿透，不写进源码、不进构建产物。
#
# 仅当将来启用「服务端密钥存储」方案（见规范 §9 后续可选）时，才在此填写：
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=sk-xxxx
# OPENAI_MODEL=gpt-4o-mini
