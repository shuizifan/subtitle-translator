# public/ 静态资源目录

把自定义 **logo 图片**、**favicon** 等放在这里，文件会以网站根路径提供。

- 例：放入 `logo.svg` → 在 `src/config/site.ts` 里把 `logoSrc` 填 `"/logo.svg"`。
- 例：放入 `favicon.ico` → 浏览器标签页图标自动生效。

详见 [docs/站点定制说明.md](../docs/站点定制说明.md)。
